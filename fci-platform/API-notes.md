Thanks for this. A few specifics on our requirements:
Request rate: 30 RPM is plenty for the pilot phase. We won't need anywhere near 100.
Input size per request: This is where the default estimate won't work for us. Our system uses a large system prompt with investigation SOPs and knowledge base documents, plus case data and conversation history. Each request will contain between 100,000 and 200,000 tokens (roughly 400,000 to 800,000 characters). This is normal for the Anthropic API with large context windows and is well within the model's 200K token limit, but it's important that the SafuAPI configuration doesn't cap request size below that.
Prompt caching: Does SafuAPI support Anthropic's prompt caching feature? Our system prompt is roughly 96,000 tokens and is identical across all requests. With caching enabled, this is sent at full cost once and then at 90% discount on every subsequent call, which significantly reduces both cost and latency. This is a big factor in keeping within the $1,000/month budget.
Models: We'll primarily use Claude Sonnet 4.6 for day-to-day use, with Claude Opus 4.6 available for more complex cases. Sonnet keeps us comfortably within budget.
Tool use and streaming: We need both tool use (function calling) and streaming responses enabled. These are standard Anthropic API features but want to confirm they're supported through SafuAPI.