  1. Install Homebrew (macOS package manager)

  This makes everything else easier:

  /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

  It will ask for your password (the laptop login password). When it finishes, it will print "Next steps" telling you to
   run two commands to add Homebrew to your PATH. Run those commands — they look something like:

  echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
  eval "$(/opt/homebrew/bin/brew shellenv)"

  Verify it works:
  brew --version

  2. Install MongoDB via Homebrew

  Forget the unzipped download — this is cleaner:

  brew tap mongodb/brew
  brew install mongodb-community

  Start MongoDB:
  brew services start mongodb-community

  Verify it's running:
  mongosh --eval "db.runCommand({ ping: 1 })"

  You should see { ok: 1 }. MongoDB will now auto-start on boot.

  3. Install Python

  brew install python@3.12

  Verify:
  python3 --version

  4. Install Node.js

  brew install node

  Verify:
  node --version
  npm --version

  5. Set up the backend

  Navigate to your project (adjust the path to wherever GitHub Desktop cloned it):

  cd ~/Documents/GitHub/fci-investigator/fci-platform

  If you're not sure where it is:
  find ~/Documents -name "fci-platform" -type d 2>/dev/null

  Create a Python virtual environment and install dependencies:

  cd server
  python3 -m venv venv
  source venv/bin/activate
  pip install -r requirements.txt

  6. Create the .env file

  cat > .env << 'EOF'
  ANTHROPIC_API_KEY=sk-placeholder-will-replace-later
  ANTHROPIC_MODEL=claude-sonnet-4-6
  KNOWLEDGE_BASE_PATH=../knowledge_base
  MONGODB_URL=mongodb://localhost:27017
  MONGODB_DB_NAME=fci_platform
  EOF

  The placeholder API key means the AI chat won't work, but the rest of the platform (login, ingestion, case list, data
  panel) will load fine.

  7. Seed the database

  python3 -m scripts.seed_demo_data

  If that gives an import error, try:
  cd ..
  python3 -m server.scripts.seed_demo_data

  Or from the fci-platform root:
  PYTHONPATH=. python3 scripts/seed_demo_data.py

  8. Start the backend

  From the fci-platform directory with the venv active:

  cd server
  source venv/bin/activate
  uvicorn main:app --reload --port 8000

  Leave this terminal running. You should see Uvicorn running on http://0.0.0.0:8000.

  9. Set up and start the frontend

  Open a new Terminal tab (Cmd+T):

  cd ~/Documents/GitHub/fci-investigator/fci-platform/client
  npm install
  npm run dev

  You should see Local: http://localhost:5173/.

  10. Open the app

  Go to http://localhost:5173 in your browser. Log in with:
  - Username: ben.investigator
  - Password: password123

  What works without an API key

  - Login, case list, ingestion dashboard (create cases, upload data, process sections, assemble)
  - Investigation page opens — case data panel, tabs, subject selector all work
  - Chat will fail when you send a message (API key invalid) — that's expected

  Once you get the API key, just edit server/.env, replace sk-placeholder-will-replace-later with the real key, and
  restart the backend (Ctrl+C then re-run the uvicorn command).

  Quick reference for daily use

  # Terminal 1 — start MongoDB (if not using brew services)
  brew services start mongodb-community

  # Terminal 2 — backend
  cd ~/Documents/GitHub/fci-investigator/fci-platform/server
  source venv/bin/activate
  uvicorn main:app --reload --port 8000

  # Terminal 3 — frontend
  cd ~/Documents/GitHub/fci-investigator/fci-platform/client
  npm run dev

  Let me know if you hit any errors at any step.





    cd ~/Documents/GitHub/fci-investigator/fci-platform
  python3 -m venv venv
  source venv/bin/activate
  pip install -r requirements.txt

  And when starting the backend:

  cd ~/Documents/GitHub/fci-investigator/fci-platform
  source venv/bin/activate
  cd server
  uvicorn main:app --reload --port 8000

  The venv lives at the fci-platform root, but you run uvicorn from server/ so the relative paths in config (like
  KNOWLEDGE_BASE_PATH=../knowledge_base) resolve correctly.


● Python 3.9.6 is the macOS system Python — too old. Recreate the venv with the Homebrew 3.12:

  deactivate
  rm -rf venv
  python3.12 -m venv venv
  source venv/bin/activate
  pip install -r requirements.txt

  If python3.12 isn't found, Homebrew may not have linked it. Check with:

  brew list python@3.12

  If it's not installed (the earlier brew install may have been skipped or failed), install it now:

  brew install python@3.12




  python scripts/manage_user.py password ben.investigator <your-password>
  python scripts/manage_user.py add jane.investigator Jane somepass123
  python scripts/manage_user.py list
  python scripts/manage_user.py delete demo.investigator

  Four commands: list, add, password, delete. Auto-generates user IDs. Delete confirms first and leaves cases intact



API Key: sk-ts02xxxxxxxxxx_xxxxxxx
Base URL: https://litellm.safuapi.secfdg.net



● Done. Four changes:

  1. server/config.py — added ANTHROPIC_BASE_URL: str | None = None. When unset, clients use the default Anthropic
  endpoint (your local dev workflow unchanged).
  2. server/services/ai_client.py — get_client() passes base_url when configured. This covers all streaming chat and
  non-streaming investigation calls.
  3. server/services/ingestion/ai_processor.py — _get_client() same pattern. This covers all ingestion batch processing
  (including Kodex).
  4. scripts/test_litellm.py — standalone test script with three checks:
    - Test 1: Non-streaming call (connectivity + auth)
    - Test 2: Streaming call (SSE through proxy)
    - Test 3: Prompt caching (two calls with cache_control — verifies the proxy passes caching headers)

  On the Binance network, your .env would be:
  ANTHROPIC_API_KEY=sk-ts02xxxxxxxxxx_xxxxxxx
  ANTHROPIC_BASE_URL=https://litellm.safuapi.secfdg.net

  If the model name gets rejected in Test 1, the script tells you to try ANTHROPIC_MODEL=anthropic/claude-sonnet-4-6 —
  that's the most common LiteLLM routing prefix.

✻ Churned for 54s

❯ How do I run the test script? Tell me exactly what to do.

● On the Binance network, from the project root (fci-platform/):

  ANTHROPIC_API_KEY=sk-ts02xxxxxxxxxx_xxxxxxx ANTHROPIC_BASE_URL=https://litellm.safuapi.secfdg.net python
  scripts/test_litellm.py

  That's it. The script reads the env vars inline — no .env file needed for the test.

  If Test 1 fails with a model name error, retry with:

  ANTHROPIC_API_KEY=sk-ts02xxxxxxxxxx_xxxxxxx ANTHROPIC_BASE_URL=https://litellm.safuapi.secfdg.net
  ANTHROPIC_MODEL=anthropic/claude-sonnet-4-6 python scripts/test_litellm.py

  You'll need pydantic-settings and anthropic installed in your Python environment there. If it's a fresh environment:

  pip install anthropic pydantic-settings